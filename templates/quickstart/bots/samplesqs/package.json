{
	"name": "quickstart-samplesqs",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"directories": {
		"test": "test"
	},
	"scripts": {
		"test": "leo-cli test . "
	},
	"config": {
		"leo": {
			"type": "cron",
			"memory": 128,
			"timeout": 10,
			"role": "QueueWriterRole",
			"env": {
				"SQS_URL": {
					"Fn::Sub": "${MyQueue}"
				}
			},
			"cron": {
				"time": "0 */1 * * * * "
			}
		}
	}
}
